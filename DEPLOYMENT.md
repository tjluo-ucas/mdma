# Deployment

The workflow in `.github/workflows/pages.yml` builds and deploys `main` to GitHub Pages. Create `tjluo-ucas/mdma`, push `main`, choose **Settings → Pages → GitHub Actions**, and verify every Chinese and English route before announcing the site.
